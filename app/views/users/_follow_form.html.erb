<% unless current_user == @user %>
  <div id="follow_form">
    <% if current_user.following? @user %>
      <%= render "unfollow", inactive_relationships: inactive_relationships %>
    <% else %>
      <%= render "follow", active_relationships: active_relationships %>
    <% end %>
  </div>
<% end %>
